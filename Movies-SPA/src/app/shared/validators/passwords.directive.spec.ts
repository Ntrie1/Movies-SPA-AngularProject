import { PasswordsDirective } from './passwords.directive';

describe('PasswordsDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordsDirective();
    expect(directive).toBeTruthy();
  });
});
